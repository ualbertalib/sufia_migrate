require "spec_helper"

describe SufiaMigrate do
  it "has a version number" do
    expect(SufiaMigrate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
